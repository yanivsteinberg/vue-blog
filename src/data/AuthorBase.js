export default class AuthorBase {
  constructor(id, firstName, lastName, pictureUrl) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.pictureUrl = pictureUrl;
  }
}
